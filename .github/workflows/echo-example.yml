name: Echo Test

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    name: Build job
    steps:
      - name: Print job name
        run: echo ${{ vars.JOB_NAME }}        
 